// public/sw.js
// This empty service worker satisfies the browser's PWA install requirement
self.addEventListener('fetch', function(event) {
  // Keeps the app installable without complex caching during development
});